pipeline {
    agent {
        docker { image 'python:3' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'python pip install --user -r vote/requirements.txt'
            }
        }
        stage('Test') {
            steps {
                sh 'nosetests -v'
            }
        }
    }
}
