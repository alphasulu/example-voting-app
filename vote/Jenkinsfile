pipeline {
    agent {
        docker { image 'python:latest' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'echo hello world'
               // sh 'pip install --no-cache-dir -r vote/requirements.txt'
            }
        }
        stage('Test') {
            steps {
                sh 'nosetest -v'
            }
        }
    }
}
