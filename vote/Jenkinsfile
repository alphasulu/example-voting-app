pipeline {
    agent {
        docker { image 'python:2.7.16-slim' }
    }
    stages {
        stage('Build') {
            steps {
                  sh "pip install -r vote/requirements.txt --user"
            }
        }
        stage('Test') {
            steps {
                    sh 'nosetests -v'
            }
        }
    }
}
